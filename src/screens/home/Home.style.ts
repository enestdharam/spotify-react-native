import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    heading: {
        fontSize: 16,
        textAlign: 'center',
        fontWeight: '700',
        margin: 15
    }
})