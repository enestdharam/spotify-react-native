import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    songImage: {
        width: 200,
        height: 200
    },
    text: {
        marginLeft: 10,
        marginTop: 10,
        fontSize: 14,
        fontWeight: 'bold'
    }
});