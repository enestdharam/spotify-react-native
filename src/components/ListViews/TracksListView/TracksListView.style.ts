import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    imageSize: {
        width: 100,
        height: 100
    },
    title: {
        marginLeft:10,
        marginTop: 10,
        fontSize:12,
        fontWeight:'bold'
    }
});